# Default values for fiware-dataspace.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  consumer: fiware-dsc-consumer01

  provider: fiware-dsc-provider01

  trustanchor: trust-anchor
